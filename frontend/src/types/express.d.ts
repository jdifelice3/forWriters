import { Group, GroupMemberRole } from "@prisma/client";


declare global {
  namespace Express {
    interface Request {
      group: Group;
      groupRole: GroupMemberRole;
    }
  }
}
import { Group, GroupMemberRole } from "@prisma/client";

declare global {
  namespace Express {
    interface Request {
      group: Group;
      groupRole: GroupMemberRole;
    }
  }import { Group, GroupMemberRole } from "@prisma/client";

declare global {
  namespace Express {
    interface Request {
      group: Group;
      groupRole: GroupMemberRole;
    }
  }
}

}
